# Railway Environment Variables Template
# Copy this file and set these values in Railway's dashboard

# Server Configuration
PORT=3000
NODE_ENV=production

# CORS Configuration
CORS_ORIGIN=*

# API Rate Limiting
API_RATE_WINDOW_MS=60000
API_RATE_LIMIT=100

# Database Configuration
# Railway will automatically provide DATABASE_URL when you add PostgreSQL
DATABASE_TYPE=postgresql
# DATABASE_URL is automatically set by Railway when you add PostgreSQL service

# For development (SQLite)
# DATABASE_TYPE=sqlite
# DATABASE_PATH=./data/fide_ratings.db

# Optional: For migration from SQLite to PostgreSQL
# SQLITE_PATH=./data/fide_ratings.db

# FIDE API Configuration (if using official API)
# FIDE_API_KEY=your_api_key_here

# Logging
LOG_LEVEL=info

# Monthly Update Schedule (cron format)
# Default: 1st day of every month at 2 AM
MONTHLY_UPDATE_SCHEDULE=0 2 1 * *