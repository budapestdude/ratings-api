# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=*

# Rate Limiting
API_RATE_WINDOW_MS=60000
API_RATE_LIMIT=100

# Database
DATABASE_PATH=./data/fide_ratings.db

# Update Schedule (Cron format - default: monthly on the 1st at midnight)
UPDATE_SCHEDULE=0 0 1 * *

# Railway will automatically set PORT
# In production, Railway provides:
# - PORT (automatically)
# - NODE_ENV=production (set manually)